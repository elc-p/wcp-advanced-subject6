$("#book_<%= @book.id %> .comments-count").html("コメント数: <%= @book.book_comments.count %>");
$(".comment").html("<%= j (render 'books/comment', {book: @book, book_comment: @book_comment}) %>");
$("textarea").val("");